node_modules
.turbo
# Any NEXT_DIST_DIR variant (.next-preview, ad-hoc test dist dirs, ...)
.next*
dist
build
coverage
test-results
playwright-report
# Deny-by-default: every .env variant is ignored, the committed template is not.
.env
.env.*
!.env.example
*.tsbuildinfo
.DS_Store
.vercel
minio-data
pgdata
chdata
redisdata
mailpitdata
