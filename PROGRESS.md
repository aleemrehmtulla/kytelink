# Progress

Orchestrator memory for the Kytelink ground-up rewrite. On compaction, re-read
`rewrite/README.md` + `rewrite/19-orchestration.md` + this file before acting.

Monorepo built in `v2/`. Legacy root is read-only parity reference.

## ✅ PROJECT COMPLETE — ACCEPTANCE MET (2026-07-19)
All 6 phases passed their gates. 20/20 acceptance criteria met (3 with recorded waivers: H12 bundle→build-tripwire, Vercel-domain automation→manual DNS, S3/S4 low-sev). Final blocker (passkey login client wiring) FIXED + virtual-authenticator E2E green + stub/stale-comment removed; full workspace typecheck green. Prime directives satisfied: no profile/login data lost (migration proven vs fixture DB), solo-user never sees org concepts (E2E), graceful degradation (degraded boots green), 9 legacy themes pixel-frozen + 3 new, zero known bugs (all P4 findings fixed/waived). Founder runs the real seed himself via v2/LAUNCH-RUNBOOK.md; orchestrator never touched real prod data. v2/ built; launch move-to-root (doc 18) + final commit are the founder's launch step.

## Phase status
- **P0 scaffold** ☑ GATE PASS (2026-07-18). v2/ monorepo builds cold, all 4 apps + CDN boot on dev(3000-3003/5002) and agent(4000-4003/5002) ports, docker 5/5 healthy, guardrails fire, CI defined. Verified personally + 2 critic cycles.
- **P1 contracts** ☑ **FROZEN 2026-07-18**. Critic panel (2×Opus) → READY TO FREEZE; all fixes applied+verified. Cold green: typecheck 13/13, lint 12/12, test 10/10 tasks, build 5/5. Frozen surface in v2/CONTRACTS.md; post-freeze changes go through orchestrator + changelog.
- **P2 build ☑ COMPLETE** — W1(core+W1b real-api, verified: readyz real pings, dev-login, OTP 000000, real-DB org.listMine, permission/limit, scheduled-publish sweep) · W2☑ · W3☑ · W4☑ · W5a☑ · W5b☑ · W6☑ · W7☑ · W8☑(migration-safe). emails render blocker FIXED (jsx classic). 
- **P3 integration ☑ GATE PASSED** (2026-07-19). Clean-boot E2E green ×3 from docker down -v (exit 0): integration 162, golden-path Playwright 38 (incl. real analytics), visual 24; degraded boots green; 24 visual baselines committed. All gate-found defects fixed WITH regression tests (414 maxParamLength; pnpm-agents worker env; 2 preview UI bugs). Workspace typecheck 14/14, lint 12/12, build 5/5. Note: E2E agent committed v2/tools/e2e to branch kyte-v2 (visual baselines need tracking); rest of v2 in working tree (untracked) — final commit + move-to-root = launch step (doc 18), user's call. FUTURE AGENTS: do NOT git commit.
- **P3 (old)** ◐. Wave 1 ☑ DONE+verified (all seams wired, Asset CRUD→PrismaStore, og-image/quarantine workers, changeEmail/unsuspend-cancel/fail-closed-limits/HMAC-body done; E2E proven: publish→moderation→og→revalidate, beacon→CH 0→12, upload→finalize→Asset+MinIO, suspend→quarantine, OTP email renders; apps/api 159/159 tests; full workspace typecheck 13/13). Clickhouse weak-type bug fixed permanently (param → Record<string,string|undefined>).
  Wave-1 BOUNDED (remaining, non-E2E-blocking): (i) PASSKEYS — RESOLUTION FOUND: install `@better-auth/passkey@1.6.23` (separate package, matches better-auth 1.6.23) + wire the passkey plugin (RP ID pinned to apex kytelink.com, localhost in dev/agent). Do AFTER Wave 2 (avoid concurrent apps/api auth edits). account.passkeys.* surface already present; W5b keeps the passkey button soft-failing until wired. (ii) app-logs→ClickHouse pino transport not wired (createAnalyticsAppLogsStream exists, not piped into logger). (iii) og-image avatarKey not threaded (og cards text-only until wired).
  Wave 2 ☑ DONE+verified: trpc.admin 6→34 procs + 4 gap fields (see CONTRACTS changelog), real resolvers, admin app wired to real API (mock removed, ignoreBuildErrors removed), agent-mode admin CORS/ADMIN_EMAILS auto-allowlist fix in api config/server. trpc/api/admin typecheck clean, api 159/159. Flags: no User.banned col (force-logout only); seed needs ModerationReview/AbuseReport/AdminAlert fixtures. Dead code: apps/admin/lib/mock-admin-source.ts + fixtures/ (safe to delete later).
  Wave 3 (RUNNING a24ed6960bd96b337) = apps/web mock→real wiring. MUST ALSO FIX: apps/web build fails (framer-motion MotionConfig→node:fs/promises Turbopack issue via app-context.tsx/invites.tsx) — Wave 3's DoD includes apps/web build green.
  Wave 3 ☑ DONE+verified: apps/web wired to REAL api by default (7 seams: tRPC client, better-auth, HMAC internal profile/domain/preview, XHR upload, beacons incl. onIconClick+username watermark). E2E proven: dev-login→editor loads real @agent kyte→edit link→publish(seq5)→public profile shows change; upload→MinIO webp; beacon→CH; preview SSR gate. web build green, 18 Playwright. Full integrated workspace: typecheck 13/13, lint 12/12, build 5/5.
  ORCHESTRATOR FIXES applied post-Wave3: (A) added kyteId to internal ProfilePayload (apps/api/src/internal/data.ts) — unblocks attributed beacons (was blocker). (B) scripts/agents.mjs now passes agent-port base URLs (WEB_BASE_URL=4000 etc. + NEXT_PUBLIC_*) so CORS/revalidate work in agent mode. (C) NEXT_PUBLIC_CDN_URL → http://localhost:9000/kytelink-assets (MinIO) in .env/.env.example so user uploads (u/) render locally (owned assets still :5002 in dev). 
  STRUCTURAL NOTE (DRY-critic): web+admin both bypass @kytelink/trpc barrel (it evaluates server appRouter→clickhouse/migrate→node:fs/promises, poisoning client bundles) — import AppRouter type-only + @trpc/client directly. Consider splitting trpc client-safe vs server exports.
  FINALIZATION agent RUNNING (a8d0303004ca99984): passkeys (@better-auth/passkey@1.6.23 + wire, RP ID apex), ModerationReview/AbuseReport/AdminAlert seed fixtures, admin dead-code deletion.
  FINALIZATION ☑ DONE: passkeys server-wired (@better-auth/passkey@1.6.23, RP ID apex/localhost, account.passkeys.* real, Passkey model already matched — no migration; endpoints 200 proven) — CLIENT passkeyClient() + WebAuthn virtual-authenticator E2E is an apps/web follow-up (web uses raw fetch, no auth-client SDK). Seed fixtures added (2 ModerationReview w/ signals, 3 AbuseReport, 2 AdminAlert; idempotent). Admin dead code deleted (mock-admin-source + fixtures/). Orchestrator fixed the 2 assets/router.test.ts failures caused by my .env CDN→:9000 change (made assertions env-derived via CDN const). apps/api 159/159 again.
  PHASE 3 GATE ◐ — E2E harness built + ran GREEN ×2 from clean (integration 159×2, golden-path Playwright 38×2, visual 24×2; degraded boots all pass; 24 visual baselines committed, commit 4db1769). Golden path: 12 assertions real vs live stack; several UI click-throughs covered by the 159-test integration matrix; passkey virtual-authenticator = follow-up.
  DEFECTS found by gate + FIXED by orchestrator: (1) **414 URI Too Long** — Fastify default maxParamLength:100 rejected the editor's batched analytics tRPC GET (~130 chars) → Analytics tab dead. FIXED: server.ts maxParamLength:5000 (verified long path routes→401 not 414). (2) **pnpm agents/dev profiles 404** — Next render workers don't inherit parent env, so getStaticProps had empty INTERNAL_API_SECRET. FIXED: scripts/run-apps.mjs writeAppServerEnv() writes gitignored apps/*/.env.local (server vars) — wired into dev.mjs+agents.mjs, cleaned on shutdown.
  E2E agent RE-RUNNING (a997ea1acdd7b7de7) to: un-mark analytics test.fail()→real pass (414 fixed), add 414 regression test, fix 2 minor preview bugs (LIMIT_LABELS key mismatch → "this"; preview URL base-join), re-run gate green ×2. Then Phase 3 gate CLOSED.
  (original E2E harness spec below:) building tools/e2e/ clean-boot gate (docker down -v/up→migrate→seed→boot→suites), consolidated golden-path Playwright, authz-denial matrix, ProfileView visual baselines, degraded-mode boots; runs GREEN ×2 from clean. Agent instructed to STOP+report real product bugs (not fix). Spec: dedicated E2E-harness agent builds/consolidates: (1) clean-boot script `docker compose down -v && up -d` + wait healthy + seed(base+agent) + boot apps; (2) consolidated Playwright golden-path (17-quality §9 30-step flow: OTP+OAuth+passkey login→onboarding-to-live→edit→upload→publish→beacon→analytics→username-change→domain→2nd-kyte→switcher→invite→revoke→transfer→limits→2 schedules→preview→import→suspend→approve→Live); (3) integration authz-denial matrix; (4) visual-regression baselines (ProfileView 12 themes × mobile/desktop × content states); (5) degraded-mode boots (no CLICKHOUSE_URL, no AWS_*). GATE = green ×2 consecutive from clean. Already-proven pieces: apps/api 159 integration, web 18 Playwright, wave E2E proofs, W8 migration suite.
- **P3 integration** ☐
- **P4 adversarial fleet ☑ CLOSED** (2026-07-19). All 6 critics triaged to zero-or-waived; every fix regression-tested; clean-boot E2E gate GREEN ×2 (integration 213, e2e 44, visual 24, exit 0); sitemap served; bundle tripwire green. Post-close fix: preview-verify rate-limit re-keyed per-token (closer flagged S6 was global via SSR-proxy IP) — api 213/213 green. Details in P4-FINDINGS.md.
- **P4 (log) adversarial fleet** — ALL 6 CRITICS REPORTED. Findings + triage + fix waves in **/Users/aleemrehmtulla/repo/kytelink/P4-FINDINGS.md** (authoritative — read it). Core VERIFIED solid (authz matrix all cells, IDOR/admin-gate/suspension/SSRF/quarantine held, static-profile perf, parity, all §7 anti-bugs avoided, zero any/@ts-ignore). ~35 findings triaged: 1 XSS blocker + security/data-loss/correctness majors MUST-FIX; a few WAIVED-with-rationale (H12 bundle post-launch perf-debt if invasive; Vercel-domain automation post-launch; S3/S4 low). 
  WAVE 1 fix agents RUNNING: FA-schemas=a57f3a1b08e8eb532 (H1-H4 validation), FA-web=a73285ef207b44c9d (B1 XSS + M1 vanity + M2 ShareModal + M6 fail-open + H12-assess), FA-api-A=ae5bd2b107afaf69a (H5 publishSeq + H6/H7/L3 org.delete + H8 N+1 + M3 owners + M7 ogImageUrl + S1 auth-ratelimit + S2 /report + S5 decline + S6 preview + S7 ssrf + L1 + SH5a AI-import). 
  WAVE 1 ☑ DONE+merged clean (workspace typecheck 14/14, schemas 183, api 198): FA-schemas (H1-H4 validation, no product/seed breakage), FA-web (B1 XSS fixed+verified vs real api, M1 vanity, M2 ShareModal, M6 fail-open, L2 confirmed dev-only-safe; H12 WAIVED — profile-specific JS only ~11.8KB, bulk is shared _app runtime; added build bundle-size tripwire 230KB instead of risky rewrite), FA-api-A (all 14: publishSeq atomic [8 concurrent→distinct], org.delete guards, listMine batched, owners-mgmt, ogImageUrl, auth rate-limit 429, /report 202, decline email-match, preview timing-safe+rate-limited, SSRF IP-pin, Retry-After, AI-import path — majors live-proven).
  WAVE 2 ☑ DONE+merged (workspace typecheck 15/15, lint 12/12, test 10/10, api 213 tests): FA-api-B (D1 sched-publish→same pipeline+D3 single cache owner, M5 catch-up drain, H9 revalidate jobId dedup+concurrency, SH1 og-gate on uploads-cap, H10 admin product_events_daily MV+cached/bounded funnel, H11 sitemap+cleanup workers [nightly cron], M4 moderation BullMQ+dead-letter→adminAlert, SH2 7-group boot warnings [boot-proven], SH4 analytics FEATURE_DISABLED, D4 dead-seam delete, D5 banners strip — all regression-tested). FA-seed-docs (D2 real normalize + SH3 docs).
  PHASE 4 CLOSING: agent a5e5864e5e4d7a486 — adds apps/web /sitemap.xml serve route (worker writes bucket+Redis; serve route was the last gap) + re-runs clean-boot E2E gate ×2 (heals infra incident via docker down -v). Green ×2 → P4 CLOSED. Remaining low/waived: admin-shapes.ts banners (packages/trpc, minor), M6 done, S3/S4/Vercel-automation waived (see P4-FINDINGS).
  === PHASE 5 next: prove W8 backfill vs legacy fixture DB (real sharp-normalize now wired) → green verification report → visual-diff gallery → quarantine/asset-failure demo → LAUNCH-RUNBOOK.md (doc 18) → final 20-acceptance.md line check. Founder runs real seed himself; orchestrator NEVER touches real prod data. ===
  WAVE 2 (orig) FA-api-B=a9200c41efcfd9b82 (D1 sched-publish→pipeline+D3, H9 revalidate dedup, SH1 og-gate, H10 admin MVs+product_events MV, H11 sitemap+cleanup workers, M4 moderation BullMQ, M5 catch-up, SH2 boot-warnings, SH4 analytics FEATURE_DISABLED, D4 dead-code, D5 banners), FA-seed-docs=a44087f740a6b0ae0 (D2 seed real sharp-normalize, SH3 email docs). After: re-run Phase 3 E2E gate ×2 → P4 closed → Phase 5.
  WAVE 2 orig (after FA-api-A lands — it edits store; FA-api-B builds the publish pipeline on it): FA-api-B (apps/api workers/analytics/env/admin-queries/moderation + clickhouse migrations: D1 sched-publish→pipeline, H9 revalidate dedup, SH1 og-gate, H10 admin MVs, H11 sitemap+cleanup, M4 moderation BullMQ, SH2 boot-warnings, SH4 analytics FEATURE_DISABLED, D4 dead-code, D5 banners) + FA-seed-docs (D2 seed real-normalize, SH3 docs). Each fix + regression test. Then re-run Phase 3 E2E gate ×2.
  (old, superseded:) 6 critics vs the live integrated product: security (Opus, ae594f9caccf567e2), permissions/role×action (Opus, a115c604464684d23), parity auditor (Opus, a48bc35adfcb0ecfc), chaos bash (Opus, a37d3f7ca49613cbc), perf+scale merged (Sonnet, a77004db4280b0fd5), DRY/quality+self-host merged (Sonnet, a3feae12a3ed0b44a).
  P4 FINDINGS (collect all 6, triage as ONE batch):
  PARITY AUDITOR DONE: 22 behaviors driven, all 01-parity §7 anti-bugs NOT reproduced, SEO/themes/fonts/colors/editor/auth PASS. 2 MEDIUM FAILs (apps/web): FAIL-1 vanity domains — middleware.ts lacks vanity-host handling; legacy served kyte.bio/username as that profile (short-domain alias), rewrite redirects to landing. Fix middleware to serve /:username on kyte.bio/kyte.lol/yoyo.so/downsad.com (root/→kytelink.com). FAIL-2 ShareKyteModal not built (SHARE_DOMAINS const unused); build modal (copy URL kytelink/kyte.bio/kyte.lol + twitter/whatsapp/linkedin/email) + editor entry point. Low/accepted notes: onboarding trigger now "no kyte in any org"; seed avatars don't resolve to MinIO objects (fallback initials — code correct). Each attacks the running app; reports ranked findings. TRIAGE all CONFIRMED → fix agents (with regression tests per defect policy) → re-run Phase 3 E2E gate. Then Phase 5. NOTE: critics may contend for agent ports 4000-4003 (each boots the product) — expect some to boot on alt ports.
- **P5 seed proving + launch package** ◐ — PROVING ☑ DONE: backfill vs legacy fixture DB w/ REAL sharp-normalize → verification 100% PASS (22 users, Kyte.id=userId, vcf dropped, no analytics, avatar-losslessness, beacon-set, quarantine-accounted, banned-preserved); resumability+delta+idempotency re-proven; real webp/LQIP HEAD-verified in MinIO. FOUND+FIXED a real migration bug: dropped link-images wrote emoji:"" → fails profileContentSchema (editor would reject) → now omits emoji + added content-parseable launch-gate check + regression test. Artifacts: tools/seed/.backfill-state/manifests/visual-diff-gallery.html (22/22 IDENTICAL), quarantine/seed-sweep/real-asset demo scripts, assets-failed.jsonl. LAUNCH-RUNBOOK.md (348 lines: per-step owner=founder, exact cmds, abort criteria, rollback, 15-min downtime, seam table, 5 founder-must-do gaps G1-G5). Added launch-seed-sweep entry point. seed 18 tests, api 213, workspace typecheck green.
  FINAL ACCEPTANCE AUDIT DONE (af03818cb56560ed9): 19/20 PASS + 3 recorded waivers (H12 bundle, Vercel automation, S3/S4). ALL workspace gates green (typecheck 15/15, lint 12/12, test 10/10, build 5/5), clean-boot E2E gate GREEN (integration 213, e2e 44, visual 24), degraded boots green, seed-verify 100% PASS. ONE blocker: criterion 4 passkey LOGIN not wired client-side (apps/web/lib/auth/auth.ts:50-53 returns null; SERVER passkey + management fully work — client WebAuthn ceremony was deferred). Fixing (not waiving — acceptance line requires it): agent aa06800a10a76a05e wires passkey login+register client ceremony + virtual-authenticator E2E. After green → ACCEPTANCE MET → project ship-ready.
  (orig audit af03818cb56560ed9): walks 20-acceptance.md line-by-line vs running product + workspace gates + E2E gate + prime directives → PASS/FAIL/WAIVED + final verdict. If MET → project ship-ready; if blockers → triage.
  (orig P5 agent ac922ae0b8dcf6d92): full backfill proving vs legacy fixture DB w/ REAL sharp-normalize → verification report + visual-diff gallery + quarantine/failure/seed-sweep demo + LAUNCH-RUNBOOK.md. Then: FINAL acceptance auditor walks 20-acceptance.md line-by-line vs the finished product (fresh Opus auditor). Orchestrator NEVER runs real prod data; founder executes the real seed himself.
  NOTE: infra needs docker up+migrate+seed after any gate `down -v` (gate teardown leaves docker down). Standard recovery: docker compose up -d → prisma migrate deploy → clickhouse migrate → AGENT_MODE seed.

## Environment facts
- Node v26.4.0, pnpm 11.9.0, Docker 29.6.1 (running, no containers up yet).
- Founder unavailable this run. Nothing open in 21-questions.md.

## Model routing (per founder directive)
- Sonnet 5: Phase 0, W2, W3, W4, W6, W7, routine per-stream critics.
- Opus 4.8: Phase 1 contracts, W1, W5a, W5b, W8, and ALWAYS security critic,
  permissions critic, parity auditor, chaos bash. Never downgrade those four.
- Sonnet stream failing 2 consecutive critic cycles on same area → relaunch fix on Opus.

## Decisions log      (date — decision — rationale — doc affected)
- 2026-07-18 — PROGRESS.md placed at repo root (new file, not legacy) — orchestrator memory visibility.
- 2026-07-18 — 3 new ThemeKey identifiers frozen as `midnight`/`dusk`/`paper` (display "Midnight"/"Dusk"/"Paper") — contract needs stable keys now; W5a designs the actual theme-object visual values in Phase 2 (doc 14). Legacy 9 keys (dark/default/spacegray/popsicle/froggy/lavender/gradientblue/gradientpink/gradientgreen) ported verbatim. — doc 03/14.
- 2026-07-18 — Phase 1 sequenced internally C1(schemas+db)→C2(clickhouse/trpc/emails/ui-surface) rather than parallel — C2 imports packages/schemas types (ProfileContent/Role); serializing kills contract drift. Both on Opus per routing. — doc 19.
- 2026-07-18 — C1 arbitration rulings: (a) User.role split into PlatformRole{USER,ADMIN} (doc 03's inline comment says "USER|ADMIN"); membership Role stays the OWNER..VIEWER ladder — APPROVED. (b) User.emailVerified → **Boolean @default(false)** overriding doc 03's DateTime? — better-auth 1.x Prisma adapter requires Boolean; this is login-enabling, so it wins; W8 seed sets true for migrated (already-verified) users. Minor doc-03 deviation. (c) analytics capability gates on CLICKHOUSE_URL only (matrix in doc 25 is authoritative; CLICKHOUSE_PASSWORD is a companion var, not a gate) — APPROVED. (d) capability booleans mean "automated/external mode available"; false = degraded fallback that STILL FUNCTIONS (email→console/mailpit, domains→manual DNS UI). Consumers render the fallback UI, never hide the feature. C1's false-for-console/manual reading STANDS with this documented semantic. — doc 03/25.

## Founder waivers
(none)

## Phase 2 architecture ruling (2026-07-18) — how streams interact with frozen packages
- **packages/trpc stays FULLY FROZEN** as the pure contract (zod I/O, AppRouter TYPE, error codes, client factory, context type, procedure-builder middleware slots + NOT_IMPLEMENTED stub bodies as the type anchor). Streams do NOT edit it. The REAL router with resolvers is built in **apps/api/src/routers/** and a type-conformance check asserts it satisfies packages/trpc's frozen AppRouter. Clients (W5b/W6/W7) build against the frozen AppRouter type NOW.
- **packages/schemas + packages/db: FULLY FROZEN** (orchestrator-only; changes via CONTRACTS.md changelog).
- **Bodies streams DO fill in Phase 2 (signatures frozen):** packages/clickhouse query-helper bodies → W2; packages/ui skeleton bodies (ProfileView render, chart internals) → W5a; packages/emails template bodies → W5a; packages/cdn/assets content → W6. Changing any FROZEN signature still goes through orchestrator.
- **apps/api dir ownership:** W1 = apps/api minus the 3 subdirs + the router assembly (imports W2/W3/W4 sub-routers via seam interfaces W1 defines); W2 = apps/api/src/analytics; W3 = apps/api/src/assets; W4 = apps/api/src/moderation. No two writers per dir.
- **Seams:** W1 ships `pnpm mock-api` day-one (unblocks W5b/W6/W7); W5a ships ProfileView+chart skeletons (already exist from C2); W8 imports W3's exported sharp-normalize module (never copies); W1's app-router assembly imports W2/W3/W4 sub-routers.
- **Batching:** 2a = W1,W5a,W5b,W6,W7,W8 (parallel now). 2b = W2,W3,W4 after W1's apps/api skeleton + seam interfaces land (they plug into it; avoids 4-way concurrent writes to apps/api shared files).

## Contract changelog (post-freeze only)
(none — not yet frozen)

## Phase 1 build status (pre-freeze)
- C1 (schemas+db+seed) DONE + verified: 148 schemas tests, migration+seed applied (20 kytes, 12 themes, org_agency_demo full, partial-unique index). emailVerified→Boolean applied (migration 3).
- C2 (clickhouse+trpc+emails+ui-surface) DONE + verified: CH DDL applied (raw+6 daily MVs, rollups proven), 59 tRPC procedures across 13 routers, 5 email shells, ui frozen surface (ProfileView props, 6 charts, footer nav-config, motion presets, DefaultSeo). Workspace cold typecheck 13/13, build 5/5 green.
- Orchestrator glue applied: apps/web/pages/index.tsx → new ProfileView `content` prop; packages/config tailwind accent → sky-500 #0EA5E9 (doc 14). Full shadcn token set deferred to W5a Phase 2.
- OPEN contract flags for critic panel to weigh (candidates to move into packages/schemas before freeze): domain-status enum + DNS-record shape (in trpc/domains); import-proposal shape (in trpc/import); env CLICKHOUSE_USER/CLICKHOUSE_DATABASE (read optionally by clickhouse, not in env schema); several tRPC output shapes (org/kyte summary, member, audit-entry, presence, passkey) live at trpc layer not schemas. platform_daily uses AggregatingMergeTree+uniqState for uniq_kytes (SummingMergeTree can't do uniques) — APPROVED deviation.

## Phase 1 critic panel (both Opus) — verdicts: READY TO FREEZE after fixes
Architecture critic: parity coverage PASS, 59 tRPC procs match doc-06, all 15 models + enums + CH tables present, permission matrix single-sourced & matches doc-04 cell-for-cell, zero `any`, seed exercises every edge. Scale/security critic: all 48 indexes cover hot queries (EXPLAIN = index scans), CH ORDER BY/partition/TTL/async correct, token-hash storage correct, capabilities correct, env required=exactly 7.
Fix cycle before freeze (C1 wave then C2 wave):
- [MAJOR arch] ProfileContent↔Prisma drift guard: was comment-only. → shared PROFILE_CONTENT_COLUMNS const + db guard test (C1).
- [MAJOR sec] beacon-per-kyte subject must be composite ip+kyte (was "kyte" → would cap popular kyte at 30 views/min total). → add composite subject (C1).
- [MINOR sec] username-check subject ip→user (C1). CLICKHOUSE_USER/DATABASE → env schema (C1).
- [MINOR sec] preview.list/kyte.get output raw `token` — must not return/store raw token → opaque id/masked (C2).
- [MINOR arch] move domainStatus/dnsRecord/importProposal shapes trpc→schemas (C1 adds, C2 re-imports).
- [WATCH → W2 brief + CONTRACTS.md] beacon carries client-supplied kyteId; W2 MUST resolve/verify kyteId from username server-side (Redis set per doc 07) — never trust beacon kyteId. No contract code change.

## FOUNDER REVIEW REQUIRED (flag, non-blocking for build)
- Legal text (Terms + Privacy) on apps/landing must be reviewed by the founder before launch — agents are not lawyers (doc 12). Data export/deletion contact address is one const the founder confirms before launch.

## Phase 2 STATUS (updated 2x) — for compaction recovery
W7 admin DONE (19 routes, green build, real capability gating, mock-driven). TWO follow-ups:
1. **Admin tRPC surface gap (contract expansion needed):** frozen trpc.admin has only ~6 procs; 13-admin needs ~20+ (overview/live/funnel stats, user mgmt searchUsers/userDetail/setUserLimits/banUser/forceLogoutUser, org/kyte detail + suspend/unsuspend/ban/unban/forceReReview/flag/deleteAsset, suspendedList, abuseReports/actionAbuseReport, auditLog [shapes.ts already has auditEntrySchema], teamsInvites, storage, trafficBreakdown, resolveAlert, logs, globalSearch). W7 typed ALL in apps/admin/lib/admin-source.ts — use as the spec. TODO: expand packages/trpc admin contract + W1 implements resolvers. Coordinate AFTER W1b (during a W1 admin-completion pass).
2. **admin/next.config.js has temporary typescript.ignoreBuildErrors:true** (workaround for the clickhouse type error) — REMOVE after clickhouse cast fix propagates + verify admin build honestly typechecks.
Clickhouse config.ts: applied definitive cast fix (`process.env as ClickhouseEnvInput`) for the bundler-resolution weak-type error that blocked web/admin/landing real-tRPC typecheck. Verify at integration (W2 currently editing that package).
pnpm-workspace.yaml uses `allowBuilds:` (pnpm 11) — W7 fixed a malformed msgpackr-extract entry; now valid (sharp/prisma/esbuild/msgpackr-extract/prisma true). My redundant package.json pnpm.onlyBuiltDependencies is harmless.

## Phase 2 STATUS — for compaction recovery
DONE+verified: W5a (ui/emails, 28 tests, watermark amend applied), W6 (landing, all pages green — UX critic pending), W7 (admin, 19 routes green — UX critic pending, admin-contract-gap tracked above), W1-core (mock-api + router conformance + permissions, verified). W8 critic #1 (Opus) done: core SOUND but found B1 delta-mode BLOCKER + M1 verify-misses-avatar + M2 manifest-false-confidence → W8 fix cycle RUNNING (a3733e79f29499eea). W8 needs a 2nd clean critic pass after fixes.
IN FLIGHT builds: W1b=a4f5da98ecd523375 (completing REAL apps/api: prisma store+context+server boot+better-auth+agent-mode+workers+internal+ratelimit/CORS). W5b=a9e4a29f06c7acdd9 (web). W7=aa2112e59dc54e571 (admin). W2=a173a804397a1d6b7 (analytics+clickhouse bodies). W3=a93a56164a40c8ebd (media/assets — exports SharpNormalizeModule for W8). W4=ad8ff0407510f15b2 (moderation).
W4 moderation DONE (36 tests, deterministic checks + mocked openai both verdicts + publishSeq guard + fail-open + seed-sweep hook). Exports: createModerationSeamFromEnv(log), runSeedSweep(store,provider,log), admin-actions approveKyte/banKyte/forceReReviewKyte, createPrismaModerationStore(log). Integration notes: (a) moderation queue is in-process ConcurrencyQueue — swap to real BullMQ Worker with PROCESS_ROLE startup in W1's server.ts/index.ts at integration; (b) publishSeq stored in ModerationReview.signals JSON (no schema col — accepted); (c) W4 uses direct @kytelink/db (workers→Postgres per doc-02), not apps/api Store abstraction. ORCHESTRATOR FIXES applied (unblock integration): added @kytelink/cdn dep to apps/api + "jsx":"react-jsx" to apps/api/tsconfig.json (emails/satori .tsx imports were breaking tsc).
W5b web DONE (89 files, E2E 9 desktop + 9 mobile green per-project, static profile motion-free + beacons + SEO parity verified, solo-no-org simplicity e2e PASSES). Built against frozen AppRouter + local mock (lib/api mock seam). Integration swaps: lib/api/client.ts (mock→createKytelinkClient), lib/auth/* (→better-auth + dev-login), profile-fetch (→/internal/profiles HMAC — already branches), middleware resolveHostOwner (→/internal/domains — already branches), upload-avatar (→real XHR presign — path written), preview verify (→SSR /internal/previews).

### CONTRACT EXPANSION needed (do in packages/trpc AFTER W1b lands, then W1 implements — batch with admin expansion):
- admin router: ~20 procs (see admin-contract-gap; spec in apps/admin/lib/admin-source.ts).
- myInvite output needs `invitedEmail` (masked-email mismatch copy, doc 04). orgSummary needs `personal` boolean (progressive disclosure solo vs team). schedule summary needs the ProfileContent `snapshot` (mini previews) or a schedule.getSnapshot. kyte.checkUsername suggestions (nice-to-have).
- ALREADY DONE (W5b built before these): ProfileView onIconClick/username added (CONTRACTS changelog); clickhouse config cast fixed — both resolve W5b friction #4/#5 at integration (swap its local error-code mirror to @kytelink/trpc).

### CRITIC STRATEGY decision (2026-07-19): per-stream build correctness self-verified by orchestrator throughout (tests/typecheck/build/live-proofs spot-checked per stream). ADVERSARIAL critics concentrated where meaningful — the INTEGRATED app in Phase 3/4 — since W1-W7 built against mocks/seams. W8 (standalone-meaningful, data-integrity) already got its dedicated 2-pass paranoia critique. Phase 4 fleet (ALWAYS Opus): security critic, permissions critic (role×action on real api), parity auditor (line-by-line on running product), chaos bash — never cut. Plus per-stream flavor: UX/parity+simplicity+a11y+responsive on the wired UI, W2 analytics correctness, W3/W4 security. This honors "mandatory adversarial critique" while not wasting it on mocks.

### PHASE 3 INTEGRATION WIRING TODO (accumulated from W2/W3/W4/W8 reports — do ALL before integration E2E)
1. Swap seam stub imports in apps/api/src/routers/index.ts (or seams registration): analytics → `import {analyticsRouter, analyticsSeam, startAnalyticsBackgroundJobs, createAnalyticsAppLogsStream} from "../analytics"`; assets → `import {assetsRouter} from "../assets"` + normalize `sharpNormalizeModule`; moderation → `registerModerationSeam(createModerationSeamFromEnv(logger))`.
2. Beacon route (apps/api/src/routes/beacons.ts, W1) must build the envelope {ip,userAgent,headers,body} from the Fastify request and call analyticsSeam.ingestBeacon(kind, envelope) — the frozen AnalyticsSeam.ingestBeacon(kind,payload) carries no request metadata; W2 defined the envelope in beacon-envelope.ts.
3. Publish flow (kyte.ts, W1) must call refreshKyteMembership on publish + clearKyteMembership on unpublish/username-change (else beacon validation always takes PG fallback — correct but uncached).
4. Publish flow must call enqueueOgImageJob (W3) on publish; suspend/unsuspend flow (W4) must call enqueueQuarantineJob (W3). og-image/quarantine BullMQ Worker startup needs PROCESS_ROLE-aware wiring in server.ts/index.ts (W1b). Moderation queue is in-process ConcurrencyQueue → swap to real BullMQ Worker at integration.
5. Unify Asset persistence: W3 keeps Asset rows in its own AssetsRepository (WeakMap) because the Store abstraction has no Asset table — fold Asset CRUD into the real PrismaStore (W1b) at integration.
6. Admin procedures: platformSeries/topKytes/funnel are implemented in W2/clickhouse + computeWatermarkFunnel exported; wire into the (to-be-expanded) admin router. See admin-contract-gap above.
7. Delete W3's apps/api/src/assets/cdn-url.ts local mirror + W4's avatar-url.ts → use getCdnUrl (@kytelink/cdn dep now added).
8. satori font: apps/api/scripts/build.mjs needs a one-line copy of src/assets/fonts/ into dist (esbuild won't carry it) — for the production build only; tsx/vitest resolve via import.meta.url fine.
9. finalize/delete (assets) bypass the shared kyte middleware (frozen input is assetId only, no kyteId) — W3 replicated the suspension/permission gate by hand; verify correct at integration.
10. Re-seed dev DB (docker compose down -v && up + seed) before integration E2E — confirmed drifted to 22 kytes (build-time test mutations).
11. W8 integration cleanups (from pass-2 critic, non-blocking): W8 should IMPORT W3's apps/api/src/assets/sharp-normalize.ts (doc-08 "import not copy") instead of its duck-typed seam copy; unstale seams.ts comment; optionally gate applyAssetRewrites by checkpoint so full re-runs don't churn updatedAt (low-sev — delta path already correct).
SECURITY FIX (done): docker-compose MinIO bootstrap now scopes anonymous read to static/* + u/* via set-json policy — verified q/ → 403, u/ → 200, static/ → 200 (quarantine invariant holds). W3 flagged the old whole-bucket-download policy leaked q/.
W2 analytics DONE (56 tests, spoof-rejection proven, buffer-drain, degradation, bot-filtering). W3 media DONE (47 tests vs real MinIO, SharpNormalizeModule matches W8's interface verbatim).
Post-freeze contract amendments applied (see v2/CONTRACTS.md changelog): ProfileView +username?/onIconClick?; NEXT_PUBLIC_API_URL/WEB_URL/LANDING_URL; clickhouse config param widened (frozen-pkg bug fix); apps/api deps pre-installed (better-auth/sharp/openai/bullmq/ioredis/aws-sdk/satori/resvg/ua-parser) + pnpm onlyBuiltDependencies for native builds.
Pending critics: W5a+W6 UX/parity (Sonnet) to batch with W5b+W7; W1 permissions critic (Opus) after W1b/W1c; each stream needs 2 clean passes.

## Phase 2 live agents (batch 2a original launch, 2026-07-18)
- W1 API core (Opus) = a4f5da98ecd523375 — apps/api core + mock-api + seams + agent mode.
- W5a UI+emails (Opus) = a306db18f178ed3d2 — DONE+verified (ui 23 tests, emails 10, 3 new theme thumbnails, all 12 themes render). Applying orchestrator-authorized ProfileView amendment (added optional username?/onIconClick? props — see CONTRACTS.md changelog). Then per-stream UX/parity critic (Sonnet). Note: new-theme visuals live in packages/ui THEME_EXTRAS (accepted).
- W5b web (Opus) = a9e4a29f06c7acdd9 — apps/web (first launch ac385717 aborted at 10s, relaunched).
- W6 landing (Sonnet) = ad69bcf4f16c8cbe8 — apps/landing + cdn assets.
- W7 admin (Sonnet) = aa2112e59dc54e571 — apps/admin.
- W8 seed (Opus) = a3733e79f29499eea — tools/seed backfill+verification+fixture DB.
- BATCH 2b (NOT yet launched — wait for W1 api skeleton): W2 analytics (Sonnet, apps/api/src/analytics + packages/clickhouse impl), W3 media (Sonnet, apps/api/src/assets — exports sharp-normalize module W8 imports), W4 moderation (Sonnet, apps/api/src/moderation). Doc 10-moderation read & ready for W4 brief.
- Each stream needs: build verification by me + adversarial critic(s) → 2 clean passes. W1 gets a dedicated permissions critic (Opus). Security/permissions/parity/chaos critics ALWAYS Opus.

## Known risks / watch items
- **INFRA INCIDENT (2026-07-19, contained):** FA-seed-docs ran `redis-cli FLUSHDB` + `mc rm u/*` on the SHARED Redis/MinIO → wiped ephemeral test state (sessions/queues/uploaded test images); Postgres kyte DB UNTOUCHED. Self-heals: E2E gate does `docker compose down -v && up` + reseed from scratch, so the gate + Phase 5 start clean regardless. Seed avatars weren't real MinIO objects anyway. RULE for all future agents: NO destructive shared-infra ops (FLUSHDB, mc rm, drop non-scratch DB) — use isolated scratch resources only.
- FA-seed-docs DONE: D2 real sharp-normalize wired into backfill --execute (import-bound, proven real webp vs stub fake bytes) + SsrfGuardedImageFetcher/S3AssetStore/RedisBeaconSink; --stub-assets keeps fast fixture path. SH3 docs fixed (.env.example EMAIL_PROVIDER default=smtp→mailpit; console=stdout-only documented accurately). verify.ts asset check still reads checkpoint metadata not live HEAD (pre-existing, Phase-5 note).
- **W1 heads-up:** `packages/schemas/src/env.ts` has a dead `devOnlyEnvSchema` (strict z.enum ["true","false"]) never wired in. If W1 wires it into validation it will reject the truthy variants ("1","yes","TRUE") that `parseBoolEnv` deliberately tolerates. Reconcile or delete it.
- Phase 0 fix added `packages/db` `prepare`+`pretypecheck`/`pretest` running `prisma generate`, and a package-level `packages/db/turbo.json`. C1 (db owner) must not clobber these when replacing the placeholder schema.
- CI `working-directory: v2` paths are intentionally deferred until the launch repo-flip (doc 18) — not a bug now.

## Phase 0 completion detail
- Build agent (Sonnet) → critic1 (Sonnet, found 1 blocker + 1 major) → fix agent (Sonnet) → critic2 (Sonnet, GATE PASS).
- Blocker fixed: clean-checkout `pnpm -w typecheck` failed (db needed `prisma generate` first). Major fixed: AGENT_MODE+production guard bypassable by casing/truthy variants — hardened with `parseBoolEnv` + case-fold NODE_ENV.
- Personally verified: cold 13/13 typecheck, guard refuses all bypass variants, dev+agents boot all ports.
