# 21 — Open questions for the founder

*Orchestrator: items here are **[FOUNDER]** decisions — do not resolve them unilaterally. Everything previously asked has been answered and folded into the specs (marked "founder-confirmed" in place).*

**Nothing is currently open.** All decisions are locked: caps (10 kytes/org, 10 people/org, 3 created + 7 joined orgs per user, 3 schedules, 5 preview links, 250MB org storage, 10MB per upload, admin-raisable), roles (single OWNER/ADMIN/MANAGER/EDITOR/VIEWER ladder), auth (Google + GitHub + OTP + passkeys via better-auth, Resend from `auth@mail.kytelink.com`), moderation (OpenAI provider, fail-open, appeal = DM @aleemrehmtulla on X, **pre-launch seed sweep suspends known spam before go-live**), analytics (**legacy data wiped — counts start fresh**), tiered env (only DB/Redis/secrets required; ClickHouse and S3 optional with graceful degradation), hosting (Vercel + Render + Neon + ClickHouse Cloud, self-host-first code), seed (built against a legacy fixture DB; founder runs it at the very end with the old prod DB), vCard deleted, vanity domains kept but only kyte.bio/kyte.lol communicated. Also locked (2026-07-17): admin alerts are in-dashboard only — the platform never emails the admin; no error-tracking SaaS — server logs ship to ClickHouse `app_logs` (90-day TTL) when analytics is on; public abuse reporting via the landing-footer `/report` form only (never on profile pages), surfaced in admin as requests to suspend; product telemetry strictly separated as "internal analytics" (`product_events`/`app_logs`) from user-facing Kytelink analytics; data export = email request, stated in the privacy policy; `www` → apex stays a Vercel-level redirect.

Also locked (2026-07-17, round 2): the monorepo is built in `v2/` of the legacy repo, moved to root at launch (legacy root is read-only reference); ~25k kytes today (small user count, heavy per-page traffic) — spec numbers reflect that; suspension = full read-only lockdown (locked editor screen, `KYTE_SUSPENDED` on every mutation, schedules held, images quarantined under a never-public `q/` prefix; data preserved; admin approve restores everything); preview links are never public (generated 6-digit passcode + deterministic checks at creation); link imports from Linktree/Beacons/Bio.link (deterministic parsers) + any URL via AI (shares the OpenAI config; user-confirmed proposals only); 3 new themes added, legacy 9 pixel-frozen; watermark → `kytelink.com/?ref={username}` (never a /signup deep-link) with `watermark_click` + funnel attribution %; OG images replaced never accumulated, and they count toward org storage; raw analytics events kept forever (no TTL); beacon validation falls back to Postgres on a Redis-set miss; username changes rate-limited (5/day), no reservation hold; passkey RP ID pinned to the apex `kytelink.com`; no QR codes, no stats emails.

If a genuinely new decision emerges during the build, add it here with a recommendation, pause only the affected stream, and continue everything else.
